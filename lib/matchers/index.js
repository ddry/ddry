// Generated by CoffeeScript 1.12.3
(function() {
  'use strict';
  var helpers;

  helpers = require('./helpers');

  module.exports = {
    "default": function(spec, specSet) {
      var _;
      return _ = {
        actual: helpers.getActual(spec, specSet),
        expected: spec.expected
      };
    },
    anyOrder: function(spec, specSet) {
      var actual;
      actual = helpers.getActual(spec, specSet);
      return helpers.anyOrder(actual, spec.expected);
    },
    plain: function(spec, specSet) {
      var _;
      return _ = {
        actual: spec.input,
        expected: spec.expected
      };
    },
    property: function(spec, specSet) {
      var actual, key, ref, value;
      helpers.getActual(spec, specSet);
      actual = {};
      ref = spec.expected;
      for (key in ref) {
        value = ref[key];
        actual[key] = helpers.getMethod(specSet.code, key);
      }
      return helpers.anyOrder(actual, spec.expected);
    }
  };

}).call(this);
