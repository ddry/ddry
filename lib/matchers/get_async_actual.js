// Generated by CoffeeScript 2.4.1
(function() {
  /*
   * Actual target code runner
   */
  'use strict';
  var dotted;

  dotted = require('../common/dotted');

  module.exports = function(spec, specSet) {
    var actual, method, promise;
    method = dotted.parse(specSet.root, specSet.methodName, false);
    promise = new Promise(function(resolve, reject) {
      return resolve(method.apply(specSet.that, spec.input));
    });
    actual = null;
    promise.then(function(result) {
      return actual = result;
    });
    return actual;
  };

}).call(this);
